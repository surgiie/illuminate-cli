includes:
    - vendor/larastan/larastan/extension.neon
    - phpstan-baseline.neon

parameters:
    paths:
        - app
        - config
        - bootstrap

    # Level 8 is the highest level
    level: 5

    excludePaths:
        - app/helpers.php
